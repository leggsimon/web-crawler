# MySQL (used by docker-compose and optionally by the backend when run locally)
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=app
MYSQL_USER=app
MYSQL_PASSWORD=

# Optional: override when running backend outside Docker
# MYSQL_HOST=localhost
# MYSQL_PORT=3306
# PORT=8080
